#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

# helpers
indent() {
  sed -u 's/^/       /'
}

# fail fast
set -e

# config
APACHE_VERSION="2.2.22"
APACHE_PATH="apache"
PHP_VERSION="5.4.9"
PHP_PATH="php"
BRANCH="master"

BIN_DIR=$(dirname $0)
BUILD_DIR=$1
CACHE_DIR=$2
LP_DIR=`cd $(dirname $0); cd ..; pwd`

# include .files when moving things around
shopt -s dotglob

cd $BUILD_DIR

# move app things to www
mkdir -p $CACHE_DIR/www
mv * $CACHE_DIR/www
mv $CACHE_DIR/www .

# keep Procfile
if [ -f www/Procfile ]; then
  mv www/Procfile .
fi

APACHE_URL="http://dl.dropbox.com/u/2726997/lib/apache.tar.gz"
echo "-----> Bundling Apache version $APACHE_VERSION"
curl --max-time 60 -sL "$APACHE_URL" | tar xz

PHP_URL="http://dl.dropbox.com/u/2726997/lib/php.tar.gz"
echo "-----> Bundling PHP version $PHP_VERSION"
curl --max-time 60 -sL "$PHP_URL" | tar xz


mkdir $CACHE_DIR/git
cd $CACHE_DIR/git

GIT="https://github.com/pateketrueke/habanero/tarball/$BRANCH"
echo "-----> Downloading framework from GitHub"
curl --max-time 60 -sL "$GIT" | tar xz

cd "$CACHE_DIR/git/$(ls $CACHE_DIR/git)"
PHPLIB="$BUILD_DIR/php/lib/php"

mkdir -p $PHPLIB/habanero
cp -R . $PHPLIB/habanero

cd $BUILD_DIR


# update config files
cp $LP_DIR/conf/httpd.conf $APACHE_PATH/conf
cp $LP_DIR/conf/php.ini php

# make php available on bin
mkdir -p bin

echo "/app/php/bin/php \"\$@\"" >> bin/php
chmod +x bin/php


# composer dependencies
COMPOSER_BIN="$CACHE_DIR/composer.phar"
COMPOSER_URL="http://getcomposer.org/composer.phar"
COMPOSER_CALL="LD_LIBRARY_PATH=$BUILD_DIR/php/ext $BUILD_DIR/php/bin/php -d date.timezone=UTC $COMPOSER_BIN"

GIT_DIR_ORIG=$GIT_DIR
unset GIT_DIR

echo "-----> Bundling Composer"
curl --max-time 60 -sL "$COMPOSER_URL" > $COMPOSER_BIN

cd "$PHPLIB/habanero"
eval "$COMPOSER_CALL install --prefer-source" | indent
cd $BUILD_DIR

if [ -f www/composer.json ]; then
  cd www
  echo "-----> Application dependencies"
  eval "$COMPOSER_CALL install --prefer-source" | indent
fi

cd $BUILD_DIR
export GIT_DIR=$GIT_DIR_ORIG


# make hs available on bin too
STACKY="habanero/bin/initialize.php"
ENVVARS="ENV=production"

printf "\r       Prepare environment and binaries... "
echo "cd /app/www && $ENVVARS /app/php/bin/php -f /app/php/lib/php/$STACKY -- \"\$@\"" >> bin/hs
echo 'done'

chmod +x bin/hs

# execute assets compilation
cd www

ENVCALL="$ENVVARS LD_LIBRARY_PATH=$BUILD_DIR/php/ext $BUILD_DIR/php/bin/php -f $BUILD_DIR/php/lib/php/$STACKY --"

printf "\r       Clean and compile assets for production... "
eval "$ENVCALL purge -cjivf && $ENVCALL prepare -cjivf" > /dev/null 2>&1
echo 'done'

cd $BUILD_DIR


cat >>boot.sh <<EOF
for var in \`env|cut -f1 -d=\`; do
  echo "PassEnv \$var" >> /app/apache/conf/httpd.conf;
done
touch /app/apache/logs/error_log
touch /app/apache/logs/access_log
tail -F /app/apache/logs/error_log &
tail -F /app/apache/logs/access_log &
export LD_LIBRARY_PATH=/app/php/ext
export PHP_INI_SCAN_DIR=/app/www
echo "Launching Apache"
exec /app/apache/bin/httpd -DNO_DETACH
EOF

chmod +x boot.sh

# clean the cache
rm -rf $CACHE_DIR/*
